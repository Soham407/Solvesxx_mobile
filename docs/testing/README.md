# Mobile Testing Docs

This folder stores active mobile test checklists that still reflect the current app behavior.

Use this folder for:

- role-specific validation checklists
- staging/device test runbooks
- focused QA guidance that still matches the live app

If a testing doc becomes obsolete, move it to `docs/archive/` instead of leaving it here.
